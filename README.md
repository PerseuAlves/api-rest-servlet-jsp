# Página web Java criada usando Servlets + JSP

Página web simples em Java (JSP) que consome uma Api Rest Java feita usando apenas os recursos padrões da linguagem (como Servlets e classes nativas de HTTP)
